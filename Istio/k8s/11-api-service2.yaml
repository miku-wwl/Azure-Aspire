# =============================================================================
# api-service2 - Backend Microservice 2
# Port: 3002
# Calls: api-service1 + PostgreSQL
# =============================================================================
apiVersion: v1
kind: Service
metadata:
  name: api-service2
  namespace: demo-app
  labels:
    app: api-service2
    version: v1
spec:
  type: ClusterIP
  ports:
    - port: 3002
      targetPort: 3002
      name: http
  selector:
    app: api-service2
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-service2
  namespace: demo-app
  labels:
    app: api-service2
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-service2
  template:
    metadata:
      labels:
        app: api-service2
        version: v1
      annotations:
        sidecar.istio.io/inject: "true"
    spec:
      containers:
        - name: api-service2
          image: demo-api-service2:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3002
              name: http
          env:
            - name: PORT
              value: "3002"
            - name: PG_HOST
              value: "postgres.demo-app.svc.cluster.local"
            - name: PG_PORT
              value: "5432"
            - name: PG_USER
              value: "devuser"
            - name: PG_PASSWORD
              value: "devpass"
            - name: PG_DB
              value: "devdb"
          readinessProbe:
            httpGet:
              path: /api/health
              port: 3002
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/health
              port: 3002
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "200m"
